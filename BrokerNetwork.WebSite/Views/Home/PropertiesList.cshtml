@model List<BrokerNetwork.Models.Model.PropertyShortDisplay>

@{
    ViewBag.Title = "الوحدات المتاحة";
}

<div class="card">
    <div class="card-header">
        <h1 class="card-title">@ViewBag.Title</h1>
    </div>
    <div class="card-block">
        @Html.Partial("_PropertiesView",Model)
    </div>
</div>