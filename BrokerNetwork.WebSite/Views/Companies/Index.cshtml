@model IEnumerable<BrokerNetwork.Models.Company>

@{
    ViewBag.Title = "شركاتى";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">@ViewBag.Title</h3>
        <div class="actions">
            <div class="dropdown actions__item">
                <i data-toggle="dropdown" class="zmdi zmdi-more-vert" aria-expanded="false"></i>
                <div class="dropdown-menu dropdown-menu-right">
                    @Html.ActionLink("اضافة شركة", "Create", null, new { @class = "dropdown-item" })
                </div>
            </div>
        </div>
    </div>
    <div class="card-block">
        <div class="table-responsive">
            <table id="data-table" class="table table-bordered">
                <thead class="thead-default">
                    <tr>
                        <th>
                            الشركة
                        </th>
                        <th>
                            مفعلة
                        </th>
                        <th>
                            تاريخ الانشاء
                        </th>
                        <th>
                            الخيارات
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <th>
                                <a href="@Url.Action("Index", "Projects", new { id = item.CompanyID })" title="@item.CompanyName">@item.CompanyName</a>
                            </th>
                            <th>
                                @Html.DisplayFor(modelItem => item.IsActive)
                            </th>
                            <td class="en-number">
                                @string.Format("{0: d/M/yyyy}", item.EntryDate)
                            </td>
                            <td class="actions-btns">
                                <a href="@Url.Action("Edit", "Companies", new { id = item.CompanyID })" title="تعديل"><i class="zmdi zmdi-edit zmdi-hc-fw"></i></a>
                                <a onclick="ShowModal('Companies/Delete/@item.CompanyID')" title="حذف"><i class="zmdi zmdi-delete zmdi-hc-fw"></i></a>
                                <a href="@Url.Action("CompanyBroker", "Companies", new { id = item.CompanyID })" title="اضافة وسطاء الشركة">اضافة الوسطاء</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>